export enum SudokuContext
{
    Input = "Input",
    Notes = "Notes",
    GameOver = "GameOver"
}

export enum DifficultyType
{
    Empty = "Empty",
    Easy = "Easy",
    Medium = "Medium",
    Hard = "Hard"
}